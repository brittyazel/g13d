post_install() {
    systemctl enable --now g13.target
    systemctl enable --now g13-resume.service
}

post_upgrade() {
    post_install
}

pre_remove() {
    systemctl disable --now g13.target
    systemctl disable --now g13-resume.service
}